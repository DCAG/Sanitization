pipeline {
  agent any
  stages {
    stage('') {
      steps {
        powershell(script: 'Write-Host "Hello"', encoding: 'utf8', label: 'Hello', returnStatus: true, returnStdout: true)
      }
    }
  }
}